# --- core scientific stack ---
numpy>=2.1,<3
scipy>=1.14
pandas>=2.2
matplotlib>=3.9

# --- vision (contrib build => cv2.aruco + GUI windows via OpenCV's bundled Qt) ---
opencv-contrib-python>=4.10

# --- robot arm ---
pydobot>=1.3
# pyserial is pinned to ==3.4 by pydobot; do not add a constraint here or
# pip refuses to resolve.

# --- notebooks / live view ---
jupyterlab>=4.2
ipywidgets>=8.1
ipympl>=0.9
flask>=3.0

# --- misc ---
pyyaml>=6.0
tqdm>=4.66
pytest>=8.0
